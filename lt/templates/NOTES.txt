LiveTrivia deployed to namespace {{ .Release.Namespace }}.

Services:
  nginx (entry point): {{ include "lt.fullname" . }}-nginx  ({{ .Values.nginx.service.type }} port {{ .Values.nginx.service.port }})
  api:                  {{ include "lt.fullname" . }}-api    (ClusterIP :8000)
  app:                  {{ include "lt.fullname" . }}-app    (ClusterIP :7777)
  postgres:             {{ include "lt.fullname" . }}-postgres (ClusterIP :5432)
{{- if .Values.sglang.enabled }}
  sglang:               {{ include "lt.fullname" . }}-sglang (ClusterIP :30000)
{{- end }}

{{- if eq .Values.nginx.service.type "LoadBalancer" }}
Get the external IP:
  kubectl get svc --namespace {{ .Release.Namespace }} {{ include "lt.fullname" . }}-nginx -w
{{- else }}
Port-forward to access locally:
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "lt.fullname" . }}-nginx 8080:80
  Then visit http://127.0.0.1:8080
{{- end }}
