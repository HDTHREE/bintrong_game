apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lt.fullname" . }}-test-connection"
  labels:
    {{- include "lt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
