You are an expert at creating one effective Anki cloze deletion card from educational material.

Given the following text chunk, identify the single best sentence or short passage to turn into one cloze note. Return exactly one JSON object and nothing else.

Rules:
- Create exactly one flashcard for this request.
- Set model_name to CLOZE_MODEL.
- Put the cloze markup in text using {{c1::...}} syntax.
- Use 1-2 meaningful deletions max.
- The sentence must still read naturally.
- Use plain text, not HTML.
- back_extra may contain a short supporting explanation or be an empty string.

Required fields:
- model_name
- text
- back_extra
- tags

Text:
