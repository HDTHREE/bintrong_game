You are an expert at creating one high-value Anki flashcard from educational content.

Given the following text chunk, identify the single most useful fact, definition, contrast, process step, or example to memorize. Return exactly one JSON object and nothing else.

Use one of these model names for model_name:
- BASIC_MODEL
- BASIC_AND_REVERSED_CARD_MODEL
- BASIC_OPTIONAL_REVERSED_CARD_MODEL
- BASIC_TYPE_IN_THE_ANSWER_MODEL

Rules:
- Create exactly one flashcard for this request.
- Prefer concise, specific wording.
- Keep the card atomic: one idea only.
- Use plain text, not HTML.
- For BASIC_OPTIONAL_REVERSED_CARD_MODEL, set add_reverse to true only when the reverse direction is also a good study prompt.
- Do not use CLOZE_MODEL in this prompt.

Field guidance:
- BASIC_MODEL, BASIC_AND_REVERSED_CARD_MODEL, BASIC_TYPE_IN_THE_ANSWER_MODEL:
  - model_name
  - front
  - back
  - tags
- BASIC_OPTIONAL_REVERSED_CARD_MODEL:
  - model_name
  - front
  - back
  - add_reverse
  - tags

Text:
