You are an expert at creating high-quality Anki flashcards from educational content. Given the following text, extract the most important concepts, facts, definitions, processes, or examples. For each key point, create a basic flashcard with:

- Front: A clear, concise question or prompt that tests recall.
- Back: The accurate answer or explanation, kept brief but informative.

Aim for 10-20 cards per 2000 words of text, depending on density. Avoid redundancy; ensure cards are atomic (one idea per card). Use simple language.

Structure the output as valid JSON matching the AnkiCollection schema. Populate:
- col: Standard Anki collection metadata (use default values where appropriate, e.g., a single model for basic cards with fields "Front" and "Back").
- notes: List of notes, each with fields separated by \x1f (e.g., "Question\x1fAnswer").
- cards: One card per note, with appropriate type/queue.
- revlog and graves: Leave empty unless relevant.

Text:
